# 과학 포트리스 — 구현 범위 판정 및 개발 계획서

> 기준 문서: `science_fortress_prd.md` (PRD), `Science_Fortress_Physics_Arcade.pdf` (인터페이스 구성참고안, 14면)
> 기준 코드: 없음 (신규 프로젝트). 같은 저장소의 **Ion Lab 3D**를 기술적 선례로 참조한다.
> 작성일: 2026-07-16 · 갱신: 2026-07-17

---

## 진행 현황 (2026-07-17)

**Phase 0~9 구현 완료 + 플레이테스트 수정 4건 반영([9절](#9-플레이테스트-후-반영한-수정-2026-07-17)).**
**순수 로직 단언 103개 + 앱 스모크 16개 전항 통과. 25조합 × 8개 리스폰 표본 클리어 실증 통과.**

| Phase | 상태 | 산출물 |
| :---: | :---: | :--- |
| 0 — 의사결정 | ✅ | [5절](#5-확정이-필요한-의사결정) 6개 항목 전부 권고안대로 확정 |
| 1 — 물리 코어 | ✅ | `physics.js` (순수·결정론). `tests/physics.test.html` 61 |
| 2 — 맵·지형·탄환·난이도 | ✅ | `maps.js`. `tools/calibrate.html`로 25조합 클리어 실증 |
| 3 — 렌더러·UI | ✅ | `renderer.js`·`index.html`·`style.css` (PDF p.5 레이아웃) |
| 4 — 점수 | ✅ | `scoring.js`. `tests/logic.test.html` |
| 5 — 진단·피드백 | ✅ | `diagnose.js`(반사실)·`feedback.js`. `tests/logic.test.html` 38 |
| 6 — 모드 4종 | ✅ | `app.js` (연습·스테이지·챌린지·시연) |
| 7 — 랭킹·로그·내보내기 | ✅ | `ranking.js` (mode 필드, 학급 랭킹 삭제, JSON/CSV) |
| 8 — 사운드·접근성·반응형 | ✅ | `sound.js` (WebAudio 합성). 키보드 조작·색 비의존 |
| 9 — 검증 | ✅ | `tests/smoke.test.html` 16 (실제 앱 구동), 헤드리스 스크린샷 확인 |

계획 대비 조정한 결정은 [8절](#8-구현-중-조정한-결정) 참고.

---

## 0. 판정의 전제

기술적으로 **구현이 불가능한 기능은 사실상 없다.** 전부 웹 브라우저에서 만들 수 있다. 따라서 이 문서에서 "구현 불가"는 다음 두 가지만을 뜻한다.

1. **요구사항끼리 충돌해서, 먼저 정하지 않으면 구현할 수 없는 것** — 대부분 여기 해당한다.
2. **서버 없이는 구조적으로 성립하지 않는 것** — localStorage의 한계에서 나온다.

그래서 이 절이 계획서의 절반을 차지한다. 아래 충돌을 정리하지 않고 코드를 시작하면 Phase 3쯤에서 되돌아와야 한다.

### 0.1 PRD 내부에서 서로 충돌하는 지점

#### (1) 난이도(Level)와 맵(Map)이 같은 변수를 두 번 정의한다 — **가장 치명적**

| 문서 | 정의 |
| :--- | :--- |
| [PRD §6](science_fortress_prd.md) | Level 3 = 마찰 O, 바람 O, **공기 저항 X** |
| [PRD §7](science_fortress_prd.md) | Map 4 = **공기 저항 O** |
| [PRD §5.2](science_fortress_prd.md) | 스테이지 모드 = 5개 맵을 순서대로 클리어 |
| [PRD §11.2](science_fortress_prd.md) | 랭킹에 "**선택** 난이도" 저장 → 난이도는 플레이어가 고르는 값 |

**학생이 Level 3을 선택하고 스테이지 모드로 Map 4에 들어가면 공기 저항은 켜지는가, 꺼지는가?** PRD는 두 답을 동시에 준다. 게다가 §6의 Level 이름(진공 훈련장 / 기본 지형 / 바람 부는 언덕 / 저항의 숲 / 종합 미션)과 §7의 Map 이름(훈련 평원 / 모래 협곡 / 바람 절벽 / 저항의 숲 / 최종 요새)은 거의 1:1로 겹친다. 같은 것을 두 번 쓴 결과다.

**권고안 — 두 축을 직교시킨다.**

- **맵**은 *어떤* 변인이 켜지는지 정한다 (정성적). Map 3에는 바람이 있고 Map 1에는 없다.
- **난이도**는 그 변인이 *얼마나 세게* 작용하고 발사 횟수를 얼마나 주는지 정한다 (정량적). Level 1이면 Map 3의 바람도 약하고, Level 5면 강하다.

```js
// 맵이 변인의 on/off를 정하고, 난이도가 배율을 정한다
const effectiveWind = map.wind * DIFFICULTY[level].envScale   // 0.5 ~ 1.5
const shots = map.maxShots + DIFFICULTY[level].shotBonus      // +2 ~ -1
```

이렇게 하면 §11.2의 "선택 난이도"도, §10.5의 난이도 배율(×1.0~×1.5)도, §5.2의 5개 맵 순차 진행도 전부 살아난다. 대신 **§6의 Level 이름 5개는 폐기한다** (맵 이름과 중복이므로). §6 표는 "Level = 난이도 세기"로 다시 쓴다.

대안(채택하지 않음): Level을 Map과 동일시한다. 간단하지만 "난이도 선택" 기능 자체가 사라지고 §10.5 배율이 무의미해진다.

#### (2) 점수 공식이 세 개다

| 출처 | 공식 |
| :--- | :--- |
| PRD §10.3 | 기본 + 남은횟수×100 + 정확도 + **난이도(덧셈)** + 연속클리어 |
| PRD §10.6 | (기본 + 남은횟수×100 + 정확도) **× 난이도배율**, 연속클리어는 총점에만 |
| PDF p.10 | [기본 1000] + [남은 발사 보너스] + [정확도] **× [난이도 배율 1.0~1.5]** = [총점] |

§10.6과 PDF p.10이 일치하므로 **§10.6을 채택하고 §10.3은 폐기한다.**

남는 문제: §10.2가 요소로 든 **"분석 보너스"(실패 후 조건을 조정해 성공하면 소량 보너스)가 §10.6 공식에도 PDF p.10에도 없다.** 그런데 이 보너스는 §24.2의 게임 성공 기준("무작위가 아니라 조건을 조절하며 재도전")을 점수로 직접 보상하는 유일한 장치다. 버리기 아깝다. **§5의 의사결정 항목으로 올린다.**

#### (3) 정확도의 척도가 두 개다

- §10.4는 4단계 **구간**이다 — 중심 +500 / 안쪽 +300 / 가장자리 +100 / 외부 0
- §16.1은 `"bestAccuracy": 92` — **연속 백분율**이다

둘 다 만족시킬 수 있다. 내부적으로는 연속값으로 계산하고, 점수만 구간으로 환산한다.

```js
accuracy = max(0, 1 - 착탄거리 / 목표물반지름)   // 0.0 ~ 1.0 → 기록·표시용
bonus    = accuracy >= 0.8 ? 500 : accuracy >= 0.4 ? 300 : accuracy > 0 ? 100 : 0
```

#### (4) 탄환이 5종인데 UI 슬롯은 3개다

- PRD §8.1: 기본 / 무거운 / 가벼운 / 둥근 / 넓적한 — **5종**
- PDF p.5 하단 컨트롤 바: `ROUND` / `FLAT` / `HEAVY` — **3종**

PRD의 5종은 축이 섞여 있다. 질량 축(무거운·가벼운·기본)과 모양 축(둥근·넓적한)이 한 줄에 나열되어 "둥근 탄환의 질량은 얼마인가"에 답할 수 없다. **PDF의 3종을 채택하고, 각각을 (질량, 항력) 쌍으로 정의한다.**

| 탄환 | 질량 | 항력 | 학생에게 보이는 설명 |
| :--- | :---: | :---: | :--- |
| `ROUND` (둥근) | 2 | 0.3 | 공기를 잘 가르며 안정적으로 날아간다 |
| `FLAT` (넓적한) | 2 | 1.0 | 공기 저항을 많이 받아 멀리 가지 못한다 |
| `HEAVY` (무거운) | 5 | 0.5 | 같은 힘을 주어도 운동 변화가 작다 |

질량 축(HEAVY vs ROUND, 항력 유사)과 모양 축(FLAT vs ROUND, 질량 동일)이 각각 **한 변인만 다르게** 설계되어 있다. 변인 통제(§2.2)를 탄환 선택만으로 실습할 수 있다.

#### (5) 바람이 스칼라인데 상승·하강 기류를 요구한다

- §9.3: `windForce = -3 ~ +3` — 1차원 스칼라다
- §6.3과 PDF p.7: 좌풍 / 우풍 / **상승 기류** / **하강 기류**

수직 성분을 표현할 수 없다. `wind: { x, y }` 2차원 벡터로 바꾼다. **§22 맵 데이터의 `wind: 1.5` 표기도 전부 `wind: { x: 1.5, y: 0 }`로 수정해야 한다.**

#### (6) 궤적 예측선이 학습 목표를 무너뜨린다

- §6.1 "궤적 예측선 일부 표시 가능", §5.1 연습 모드 "궤적 표시 강화"
- 그러나 PDF p.6의 게임 루프 2단계는 "**예측** 및 발사 — 가설 실행"이고, §24.1의 학습 성공 기준도 "예측과 검증"이다

**완전한 예측선을 그려주면 학생은 물리를 이해할 필요 없이 선만 목표물에 맞추면 된다.** 게임은 쉬워지고 학습은 사라진다.

권고: 발사 **전**에는 발사대 부근의 짧은 조준 가이드(초기 방향과 세기만, 중력 미반영)만 그린다. 발사 **후**에는 실제 궤적을 점선으로 남기고 누적한다 (PDF p.5·§13.2가 요구하는 것도 이쪽이다). 전체 예측선은 **연습 모드 전용 토글**로만 허용한다.

### 0.2 서버 없이 성립하지 않는 것

**§18.1이 랭킹 저장을 localStorage로 못 박았다. localStorage는 브라우저·기기별로 격리된다.** 여기서 두 기능이 무너진다.

| 기능 | 왜 불가능한가 |
| :--- | :--- |
| §16.3 **교사용 활용** ("학생이 실패 후 조건을 조정했는가"를 교사가 확인) | 학생 기기의 localStorage를 교사 기기에서 읽을 방법이 없다. §16.2의 발사별 로그를 아무리 잘 쌓아도 교사에게 도달하지 않는다. |
| §13.4 **학급 랭킹** | 같은 이유. 교실 공용 PC 한 대에서만 의미가 있고, 1인 1기기 환경에서는 "학급"이 성립하지 않는다. |

§18.2가 교사용 대시보드를 이미 MVP에서 제외했으므로 §16.3은 자연히 v2다. 그러나 **§13.4의 "학급 랭킹"은 랭킹 화면 요소로 버젓이 남아 있다. MVP에서 제거해야 한다.**

MVP 내 절충안: 결과 화면에 **JSON/CSV 내보내기** 버튼을 둔다. 학생이 파일을 내려받아 제출하면 교사가 취합할 수 있다. 서버도, 개인정보 수집도 없이 §16.3의 목적 일부를 달성한다. 구현 비용은 함수 하나 수준이다.

### 0.3 랭킹 테이블에 모드 구분이 없다

§11.2의 랭킹 항목에 모드 필드가 없다. 그런데 §5.3의 챌린지 모드는 **랜덤·복합 조건**이다. 스테이지 모드 점수와 한 테이블에 섞이면 비교가 무의미해진다. `mode` 필드를 추가하고 랭킹 화면을 탭으로 분리한다.

---

## 1. 구현 가능 기능 (MVP 확정 범위)

PDF p.13의 MVP 체크리스트(5개 맵 & 5단계 난이도 / 슬라이더 조작 및 궤적 표시 / 로컬 랭킹)와 §18.1을 기준으로 한다.

| # | 기능 | 근거 | 비고 |
| :--- | :--- | :--- | :--- |
| 1 | 2D 발사 게임 화면 (Canvas) | §18.1, PDF p.5 | |
| 2 | 발사각 슬라이더 10°~80° | FR-004, PDF p.5 | |
| 3 | 발사 힘 슬라이더 1~100 | FR-005, PDF p.5 | |
| 4 | 탄환 선택 3종 | FR-006, PDF p.5 | 5종 → 3종. [0.1(4)](#4-탄환이-5종인데-ui-슬롯은-3개다) |
| 5 | 중력·마찰·공기 저항·바람 시뮬레이션 | §9, PDF p.7 | 바람은 2D 벡터. [0.1(5)](#5-바람이-스칼라인데-상승하강-기류를-요구한다) |
| 6 | 5개 맵 + 지형 | §7, §22 | §22에 **지형 데이터가 없다.** [4.3 Phase 2](#43-단계별-계획) |
| 7 | 난이도 5단계 | §6, PDF p.13 | 세기 배율로 재정의. [0.1(1)](#1-난이도level와-맵map이-같은-변수를-두-번-정의한다--가장-치명적) |
| 8 | 명중·실패 판정 | FR-009, FR-010 | |
| 9 | 궤적 점선 표시 (발사 후 누적) | FR-008, PDF p.5 | 예측선은 연습 모드 전용. [0.1(6)](#6-궤적-예측선이-학습-목표를-무너뜨린다) |
| 10 | 점수 계산 + 누적 점수 | FR-011, §10.6, PDF p.10 | |
| 11 | 발사 후 피드백 문구 | FR-014, §23, PDF p.11 | 진단 방식은 [4.3 Phase 5](#43-단계별-계획) |
| 12 | 3회 실패 시 힌트 | §12.3, PDF p.11 | |
| 13 | 맵 클리어 후 개념 정리 | §12.2 | |
| 14 | 결과 화면 | §13.3 | |
| 15 | localStorage 랭킹 | FR-012, FR-013, §11.4, PDF p.10 | `mode` 필드 추가. [0.3](#03-랭킹-테이블에-모드-구분이-없다) |
| 16 | 메인 메뉴 | FR-001, §13.1 | |
| 17 | 모드 4종 (연습/스테이지/챌린지/시연) | §5, PDF p.9 | |
| 18 | 발사별 로그 기록 + 내보내기 | §16.2 | 내보내기는 [0.2](#02-서버-없이-성립하지-않는-것)의 절충안 |
| 19 | 사운드 | §15.1 | WebAudio 합성. [4.2](#42-기술-스택-결정) |
| 20 | 접근성 (키보드·색 비의존) | NFR-001 | Ion Lab 3D 선례 |

**§5.4 교사용 시연 모드를 MVP에 포함한 이유** — §18.2가 제외한 것은 "교사용 **대시보드**"(데이터 분석 화면)이지 시연 모드가 아니다. 시연 모드는 PDF p.9에 4대 모드 중 하나로 명시되어 있고, PDF p.12의 수업 시나리오 Phase 1(도입)이 전적으로 여기 의존한다. 게다가 구현 비용이 거의 없다 — 이미 만든 조건 설정 UI의 잠금을 푸는 것뿐이다.

---

## 2. 구현 가능하나 MVP에서 제외 (v2 이관)

| 기능 | 근거 | 제외 사유 |
| :--- | :--- | :--- |
| 교사용 대시보드 | §18.2, §16.3 | 서버 필요. [0.2](#02-서버-없이-성립하지-않는-것) |
| 학급별 랭킹 | §18.3, §13.4 | 서버 필요. **§13.4에서 삭제할 것** |
| Firebase / Supabase 연동 | §11.4 | 학교망 외부 접속 차단 가능성 + 개인정보 검토 필요 |
| 맵 에디터 / 미션 제작기 | §18.3, PDF p.13 | |
| 활동지 자동 생성 | §18.3 | |
| 탄환 커스터마이징 | §18.3 | |
| 이동하는 목표물 | §6.5 | Map 5 요구. 정지 표적으로 8회 클리어 가능성부터 검증한 뒤 판단 |
| 보호막 뒤 목표물 | §6.5 | Map 5의 `wall` 장애물로 대체 가능. 별도 보호막 시스템은 v2 |
| 온라인 멀티플레이 / 실시간 대전 / 회원가입 | §18.2, PDF p.13 | 명시적 제외 |

---

## 3. 구현 불가 (요구사항 수정 없이는 진행 불가)

| 항목 | 문제 | 필요한 결정 |
| :--- | :--- | :--- |
| 스테이지 모드의 환경 조건 | Level과 Map이 같은 변수를 이중 정의 | [0.1(1)](#1-난이도level와-맵map이-같은-변수를-두-번-정의한다--가장-치명적) |
| 맵 렌더링·착지 판정 | §22 맵 데이터에 **지형(terrain)이 없다.** Map 2는 "목표물이 낮은 언덕 뒤", Map 3은 "절벽 건너편"인데 `obstacles` 배열의 사각형 하나로는 지면 형상을 표현할 수 없다 | 지형 폴리라인 스키마 추가 ([4.3 Phase 2](#43-단계별-계획)) |
| 지면 종류별 마찰 | §6.2가 얼음·흙·모래를 요구하나, §22 맵 데이터의 `friction`은 맵당 스칼라 하나뿐이다 | 지형 세그먼트별 `surface` 속성 |
| Map 5 클리어 가능성 | `wall`(x=600, h=180) 뒤 목표물(x=800)을 8회 이내에 맞힐 수 있는지 검증되지 않았다. 발사대 위치조차 정의되어 있지 않다 | Phase 2에서 시뮬레이션으로 실측 |
| 상승·하강 기류 | 바람이 스칼라 | [0.1(5)](#5-바람이-스칼라인데-상승하강-기류를-요구한다) |

---

## 4. 개발 계획

### 4.1 원칙

1. **물리 코어는 DOM을 모르는 순수 함수로 만든다.** Ion Lab 3D의 `rules.js`가 단독 테스트 가능했던 이유이며, 여기서는 그보다 더 중요하다 — [Phase 5의 피드백 진단이 이 성질에 의존한다](#43-단계별-계획).
2. **같은 입력은 반드시 같은 궤적을 낳는다.** §2.2의 "변인 통제"와 §5.4의 시연 모드(같은 각도, 힘만 변경)는 결정론이 깨지면 성립하지 않는다. 고정 타임스텝으로 적분하고 `Math.random()`을 물리 경로에서 배제한다. 챌린지 모드의 랜덤은 **발사 전 조건 생성 시점에만** 개입한다.
3. **외부 CDN을 부르지 않는다.** 학교 오프라인망을 전제한다 (Ion Lab 3D가 이미 확립한 제약).
4. **빌드 도구를 두지 않는다.** `index.html`을 열면 실행된다.

### 4.2 기술 스택 결정

**PRD §21.1은 React + TypeScript + Phaser.js + Matter.js를 제안한다. 이를 채택하지 않고 순수 JS + Canvas 2D + 자체 물리 적분기를 권고한다.**

PRD §9.1이 이미 "또는 **HTML Canvas 기반 커스텀 물리 구현**"을 허용하므로, 이는 PRD 위반이 아니라 PRD가 제시한 두 선택지 중 후자를 고르는 것이다.

| 논점 | 판단 |
| :--- | :--- |
| **Matter.js가 과한가** | 그렇다. 필요한 물리는 질점 + 중력 + 선형 항력 + 바람 + 지면 구름 마찰이 전부다. Matter.js는 강체 회전·충돌 매니폴드·제약 조건을 위한 엔진이고, 우리는 그중 어느 것도 쓰지 않는다. |
| **결정론** | 자체 고정 스텝 적분기는 결정론을 설계로 보장한다. Matter.js는 sleeping·resting 처리가 개입해 "같은 입력 → 같은 궤적"을 별도로 검증해야 한다. [원칙 2](#41-원칙)가 이를 요구한다. |
| **반사실 재시뮬레이션** | Phase 5의 피드백 진단은 **같은 발사를 조건만 바꿔 수십 번 다시 굴린다.** 순수 함수면 `simulate(cond)`를 그냥 다시 호출하면 된다. Matter.js는 월드를 새로 만들고 폐기해야 한다. |
| **오프라인망** | Phaser(~1MB) + Matter를 CDN 없이 쓰려면 파일을 저장소에 커밋하거나 번들러를 도입해야 한다. [원칙 3·4](#41-원칙)와 충돌한다. |
| **성능** | NFR-002가 크롬북을 요구한다. 탄환 1개를 그리는 데 게임 프레임워크는 필요 없다. |
| **선례** | Ion Lab 3D는 3D 렌더링조차 Three.js 없이 Canvas 2D 투영으로 해결했고 보급형 태블릿에서 오히려 유리했다. 2D 포물선 궤적에 프레임워크를 도입할 이유가 더 약하다. |

```
index.html          화면 구조
style.css           스타일 (외부 폰트·CDN 없음)
physics.js          [순수] 적분기·궤적·충돌 판정. DOM 모름
maps.js             [데이터] 맵 5종 · 지형 · 탄환 3종 · 난이도 5단계
scoring.js          [순수] 점수·정확도 계산
diagnose.js         [순수] 반사실 시뮬레이션 기반 실패 원인 진단
feedback.js         [데이터] 피드백·힌트·개념 정리 문구 (§12, §23)
renderer.js         Canvas 2D 렌더링 · 입력
ranking.js          localStorage 랭킹 · 로그 · 내보내기
sound.js            WebAudio 합성 (오디오 파일 없음)
app.js              상태와 UI
tests/              physics · scoring · diagnose 단위 테스트
```

**사운드(§15)** — 효과음 파일을 CDN에서 받을 수 없고 저장소에 바이너리를 넣기도 부담스럽다. 발사음·명중음·버튼음 정도는 WebAudio의 오실레이터로 합성한다 (파일 0바이트). §15.2의 스테이지 클리어 **음악**은 합성으로 감당하기 어려우므로 v2로 미룬다.

### 4.3 단계별 계획

| Phase | 산출물 | 완료 기준 |
| :---: | :--- | :--- |
| **0** | **의사결정 확정** — [5절](#5-확정이-필요한-의사결정)의 6개 항목에 답한다 | 코드 없음. 답이 나오기 전에는 Phase 2 이후가 전부 재작업 대상이다 |
| **1** | `physics.js` — 고정 스텝 적분기, 중력·항력·바람·지면 마찰, 지형 충돌 | 같은 입력 100회 → 착탄점 완전 동일. 항력 0·바람 0일 때 해석해 포물선과 오차 1px 이내 |
| **2** | `maps.js` — 지형 폴리라인 + 세그먼트별 `surface` + 맵 5종 + 탄환 3종 + 난이도 5단계 | **5개 맵 × 5개 난이도 = 25조합 전부 클리어 가능함을 시뮬레이션으로 실증.** 특히 Map 5 / Level 5의 8회 제한 |
| **3** | `renderer.js` + `index.html` + `style.css` — PDF p.5 레이아웃 (상단 우측 SCORE·SHOTS LEFT, 하단 통합 컨트롤 바) | 발사 → 궤적 점선 누적 → 명중 판정이 화면에서 동작 |
| **4** | `scoring.js` + 결과 화면 (§13.3) | §10.6 공식대로 점수가 나온다 |
| **5** | `diagnose.js` + `feedback.js` — 실패 원인 진단, 3회 힌트, 개념 정리 | 아래 상술 |
| **6** | 모드 4종 (§5, PDF p.9) | 시연 모드에서 "같은 각도, 힘만 변경" 비교가 화면에 동시 표시된다 |
| **7** | `ranking.js` + 메인 메뉴 (§13.1) — 랭킹, 발사 로그, 내보내기 | 새로고침 후에도 랭킹 유지 (NFR-003) |
| **8** | `sound.js` + 접근성 + 반응형 | 키보드만으로 전 기능 조작 (NFR-001) |
| **9** | `tests/` | physics·scoring·diagnose 단위 테스트 통과 |

#### Phase 5를 따로 설명하는 이유

§23의 피드백 문구 목록은 쉬워 보이지만, **"힘이 약해서"인지 "공기 저항 때문에"인지를 무엇으로 판정할 것인가**에 PRD는 답하지 않는다. 착탄점 하나만 보고는 알 수 없다. 두 원인은 똑같이 "짧게 떨어짐"으로 나타난다.

**반사실 시뮬레이션으로 각 변인의 기여도를 실측한다.** 물리 코어가 순수 함수이므로 같은 발사를 조건만 바꿔 다시 굴릴 수 있다.

```js
const actual = simulate(cond)                          // 실제 착탄점
const noWind = simulate({ ...cond, wind: { x: 0, y: 0 } })
const noDrag = simulate({ ...cond, airResistance: 0 })
const noFric = simulate({ ...cond, friction: 0 })

// 각 변인이 착탄점을 몇 px 밀어냈는지가 곧 기여도다
const blame = {
  wind: dist(actual, noWind),
  drag: dist(actual, noDrag),
  friction: dist(actual, noFric),
}
// 가장 큰 기여도가 임계값을 넘으면 그 변인을 지목하고,
// 아무것도 넘지 않으면 그때 비로소 힘/각도 문제로 귀결시킨다
```

이 방식은 §12.1이 요구하는 **정확한** 피드백을 주면서, §17.2의 수업 시나리오(마찰 있음/없음 비교)와 계산 코드를 그대로 공유한다. 발사 1회당 시뮬레이션 4회지만 궤적 하나가 수백 스텝에 불과해 크롬북에서도 1ms 미만이다. — [4.2](#42-기술-스택-결정)에서 자체 엔진을 택한 실질적 이유가 이것이다.

### 4.4 의존 관계

```
Phase 0 (의사결정)
   └─> Phase 1 (physics) ──┬─> Phase 2 (maps) ─> Phase 3 (renderer) ─┬─> Phase 6 (modes)
                           │                                          │
                           ├─> Phase 4 (scoring) ─────────────────────┤
                           │                                          │
                           └─> Phase 5 (diagnose) ────────────────────┘
                                                                      └─> Phase 7 (ranking)
                                                                             └─> Phase 8, 9
```

Phase 4와 5는 Phase 1에만 의존하므로 Phase 3과 병행할 수 있다. **Phase 2의 완료 기준(25조합 클리어 실증)이 사실상의 관문이다** — 여기서 맵 좌표를 다시 잡게 될 가능성이 높고, 그 여파가 Phase 3 이후 전체에 미친다.

---

## 5. 확정이 필요한 의사결정

Phase 1 착수 전에 답해야 한다.

| # | 항목 | 권고 |
| :---: | :--- | :--- |
| 1 | **Level과 Map의 관계** ([0.1(1)](#1-난이도level와-맵map이-같은-변수를-두-번-정의한다--가장-치명적)) | 맵 = 변인 on/off, 난이도 = 세기 배율. §6의 Level 이름 5개 폐기 |
| 2 | **분석 보너스** ([0.1(2)](#2-점수-공식이-세-개다)) — §10.2에 있고 §10.6·PDF p.10에 없다 | 채택. §24.2의 게임 성공 기준을 점수로 보상하는 유일한 장치다. 직전 실패 대비 조건을 **한 개만** 바꿔 성공하면 +200 (변인 통제 자체를 보상) |
| 3 | **탄환 3종 vs 5종** ([0.1(4)](#4-탄환이-5종인데-ui-슬롯은-3개다)) | PDF의 3종 |
| 4 | **예측선 범위** ([0.1(6)](#6-궤적-예측선이-학습-목표를-무너뜨린다)) | 발사 전에는 짧은 조준 가이드만. 전체 예측선은 연습 모드 토글 |
| 5 | **학급 랭킹** ([0.2](#02-서버-없이-성립하지-않는-것)) — §13.4에 있으나 서버 없이 불가 | §13.4에서 삭제. 대신 결과 화면에 JSON/CSV 내보내기 |
| 6 | **이동 표적** (§6.5) | MVP 제외. Phase 2에서 정지 표적 클리어 가능성 확인 후 재검토 |

---

## 6. 리스크

| 리스크 | 영향 | 대응 |
| :--- | :--- | :--- |
| **Map 5가 8회 이내에 클리어 불가능할 수 있다** | §7.5 클리어 조건 붕괴 | Phase 2의 완료 기준에 25조합 실증을 넣은 이유다. 좌표를 실측으로 다시 잡는다 |
| **난이도 배율(×1.0~×1.5)이 역설을 낳는다** | Level 5가 어렵지만 배율이 높아, 쉬운 난이도로 만점 클리어하는 것보다 어려운 난이도로 겨우 클리어하는 게 점수가 높을 수 있다. 학생이 실력이 아니라 배율을 고르게 된다 | Phase 4에서 최소·최대 점수를 난이도별로 실측해 배율을 조정한다. 최악의 경우 Level별 랭킹 분리 (§13.4가 이미 "난이도별 랭킹"을 요구한다) |
| **반사실 진단이 애매한 발사에서 오답을 낸다** | 여러 변인이 비슷하게 기여할 때 엉뚱한 원인을 지목 | 임계값 미달 시 변인을 지목하지 않고 §23의 `tooWeak`/`tooStrong` 같은 일반 문구로 후퇴한다. **틀린 과학 설명을 하느니 두루뭉술한 게 낫다** |
| **§2.3의 금지 범위를 UI가 침범한다** | 교육과정 이탈 | 화면에 나가는 문자열을 `feedback.js` 한 곳에 모은다. 속도 벡터·성분·공식은 렌더러가 그릴 수단 자체를 두지 않는다 |
| 슬라이더 조작이 크롬북 터치패드에서 불편 | 학생이 각도를 미세 조정 못 함 | 슬라이더 + 화살표 키 ±1 + 숫자 직접 입력. NFR-001의 키보드 요구와 동일한 대응 |

---

## 7. 열려 있는 질문

- **§6.2의 지면 종류(얼음/흙/모래)는 맵당 하나인가, 구간별인가?** 계획은 구간별(`surface` per segment)을 전제한다. 구현 비용이 사실상 같고, 시연 모드에서 한 화면에 마찰 비교를 보여줄 수 있기 때문이다. 맵당 하나면 §5.4의 "마찰 있음/없음 비교"를 두 번 발사해야 한다.
- **챌린지 모드의 랜덤 시드를 기록할 것인가?** 기록하면 "같은 조건 재도전"과 기록 검증이 가능하다. 랭킹 항목에 `seed` 한 필드를 추가하는 비용뿐이다.
- **연습 모드에서도 발사 로그(§16.2)를 쌓을 것인가?** §5.1은 "점수 기록 없음"이라고만 한다. 로그는 점수가 아니므로 쌓아도 무방하고, PDF p.12의 수업 Phase 2가 로그 기록을 전제한다.

---

## 8. 구현 중 조정한 결정

계획을 실제 코드로 옮기며 바꾼 결정들이다. 원 계획과 어긋난 부분을 남긴다.

### 8.1 물리 상수는 "교과서 값"이 아니라 "960px 세계에 맞춘 값"이다

계획은 PRD 9.3의 내부 변수(gravity 9.8 등)를 그대로 쓰려 했다. 그러나 `tools/calibrate.html`이 **Map 4·5를 25조합 전부 클리어 불가로 판정**했다. 원인은 두 가지였다.

- 진공 최대 사거리를 가장 먼 목표물(726px)에 딱 맞춰 놓아, 항력·역풍이 사거리를 깎을 여유가 0이었다. → `POWER_SCALE`를 키워 진공 사거리를 1378px로 (목표물의 약 2배) 잡았다.
- 항력 계수가 과해 PRD가 "보통"으로 상정한 `airResistance 0.02`가 사거리를 60% 넘게 깎았다. → `DRAG_SCALE`를 55로 낮췄다.
- 무거운 탄환 질량 4는 사거리가 둥근 탄환의 절반이라 어느 맵에서도 목표에 닿지 못했다. → 질량 3으로.

**교훈:** 계획서 4.3이 Phase 2의 완료 기준으로 "25조합 클리어 실증"을 못 박은 것이 정확히 이 문제를 잡았다. 이 관문이 없었다면 렌더러·UI를 다 만든 뒤 Map 5가 클리어 불가임을 발견했을 것이다.

### 8.2 반사실 진단은 "착탄점이 움직였나"가 아니라 "목표에 가까워졌나"로 판정한다

계획서 4.3의 진단 스케치는 변인을 끄고 착탄점이 몇 px 움직였는지(`dist`)만 쟀다. 그러나 약한 발사는 **마찰을 꺼도 착탄점이 앞으로 밀린다** — 지면을 굴러 목표까지 가기 때문이다. 그래서 힘 부족인 발사가 "마찰 탓"으로 오진되었다.

바로잡은 규칙: 변인을 제거한 반사실을 **끝까지 다시 굴려**, 그 결과가 명중권(반지름의 1.2배) 안에 들어오면서 목표까지의 거리를 임계값 이상 줄일 때만 그 변인을 지목한다. 마찰은 추가로 "탄환이 목표까지의 60% 이상을 날아가 착지했을 때"만 후보로 둔다 — 착지 자체가 한참 짧았다면 그건 힘·각도 문제다. ([diagnose.js](diagnose.js), [tests/logic.test.html](tests/logic.test.html)이 이 경계를 검증한다.)

### 8.3 지면에서의 수평 바람은 시연 모드에서 조심해서 쓴다

수평 바람이 지면 마찰보다 세면(내부값 약 2.0 이상) 착지한 탄환이 굴러 나가 멈추지 않는다(runaway). 맵 데이터의 바람은 모두 이 한계 아래로 잡았고, 교사용 시연 모드의 강제 바람도 ±1.6으로 제한했다. 학생용 진단 문구는 이 경우 "화면 밖으로 나갔습니다"로 안전하게 후퇴한다.

### 8.4 덤으로 추가한 것 — 헤드리스 검증 도구

Node가 설치돼 있지 않은 환경이라, 순수 로직을 브라우저 하나로 검증하는 `tools/run.py`(테스트)와 `tools/shot.py`(스크린샷)를 만들었다. `physics`·`scoring`·`diagnose`가 DOM을 모르는 순수 함수(계획서 4.1 원칙 1)인 덕분에 가능했다. `tools/calibrate.html`은 테스트가 아니라 **맵 밸런싱 도구**로 남겨, 좌표를 바꿀 때마다 25조합 클리어 가능성을 다시 실측할 수 있다.

### 8.5 계획대로 지킨 것

- **기술 스택** — React·Phaser·Matter.js 없이 순수 JS + Canvas 2D + 자체 적분기. 계획서 4.2 그대로.
- **결정론** — `simulate` 100회 반복 시 궤적이 비트 단위로 동일함을 테스트가 보장한다.
- **교육과정 금지어** — 학생 문구를 `feedback.js` 한 곳에 모으고, 금지어(벡터·삼각함수·가속도·성분·공식 등)가 없음을 테스트가 검사한다. 렌더러에는 속도 벡터를 그리는 코드 자체가 없다.
- **오프라인망** — 외부 폰트·CDN·오디오 파일 0개. `index.html`을 열면 실행된다.

---

## 9. 플레이테스트 후 반영한 수정 (2026-07-17)

실제로 플레이해 본 뒤 나온 피드백 네 가지를 반영했다.

### 9.1 굴러서 클리어되는 문제 → 목표물을 공중에 띄웠다

Map 1에서 힘·각도가 모자라도 탄환이 착지 후 굴러가 목표물에 닿으면 클리어됐다. 포물선으로 조준하는 학습 목표가 무너진다. **목표물을 지면에서 62px 띄워 기둥 위에 세웠다.** 굴러가는 탄환의 중심은 지면 바로 위(반지름 ~7px)라, 목표물 중심(지면에서 62px)과의 거리가 반지름(26px)보다 항상 커서 굴러서는 닿지 못한다. 반드시 포물선으로 띄워 넣어야 명중한다. `tests/physics.test.html`의 "수정사항 1" 절이 이를 검증한다.

### 9.2 넓적한(FLAT) 탄환 제거

탄환을 둥근·무거운 2종으로 줄였다. 셋 중 넓적한 탄환은 "모양-항력" 축을 하나 더 얹었지만 학습 초점을 흐렸다. 남은 두 탄환은 **모양(항력)이 같고 질량만 다르므로**, 탄환을 바꿔 보는 것만으로 질량의 효과(같은 힘에 덜 나가지만 바람·공기 저항에 덜 흔들림)를 변인 통제로 관찰할 수 있다. Map 4의 교훈도 "물체 모양"에서 "물체 질량"으로 다시 썼다.

### 9.3 슬라이더 → 대포 조준 (다이얼 + 뒤쪽 힘 손잡이로 재설계)

각도·힘을 슬라이더가 아니라 대포 조작으로 정한다. 두 번 고쳤다.

1. **1차(슬링샷):** 한 번의 드래그가 각도와 힘을 동시에 바꿈 → 둘이 같이 움직여 불편.
2. **2차(구역 라우팅):** 대포 근처=각도, 빈 곳=힘 → **어디가 "빈 곳"인지 알 수 없어 힘 조절이 안 됨.** 화면 구석의 대포라 감지 반경이 대부분을 덮어 전부 각도로 잡혔다.
3. **최종(눈에 보이는 손잡이 두 개):**
   - **각도** — 대포 입구 앞의 **곡선 다이얼(10°~80°)** 위 손잡이(↕)를 끌어 맞춘다. 놓으면 각도만 고정.
   - **힘** — 대포 뒤의 **힘 손잡이**를 잡아 멀리 당길수록 힘이 커진다(당긴 거리 = 힘). 놓으면 발사. 각도는 유지. 당김 중엔 충전 게이지가 찬다.
   - 두 손잡이가 서로 반대편에 떨어져 눈에 보이므로 무엇을 잡을지 헷갈리지 않는다. 손잡이·다이얼 밖을 누르면 아무 일도 없다.

각도·힘 숫자 판은 **대포 입구를 가리지 않도록 화면 좌상단(바람 상자 아래)에 고정**했다. 슬라이더는 "정밀 조절"로 접어 키보드 접근성(NFR-001)을 위해 남겼다 — ↑↓←→·스페이스로도 전 조작이 된다. 구현은 `app.js`의 `bindCannonDrag`(손잡이 위치로 판정), 시각화는 `renderer.js`의 `drawAngleDial`·`drawPowerControl`·`drawReadout`. 손잡이 좌표는 `Renderer.knobAngle`·`knobPowerRest`로 렌더러와 앱이 공유한다.

### 9.4 리스폰마다 초기 조건 랜덤화

맵 지형이 고정이라 각도·힘 값만 외우면 클리어가 너무 쉬웠다. **맵을 다시 시작할 때마다 목표물 거리(모든 맵)와 바람 방향·세기(바람 있는 맵)가 새로 정해진다.** `Maps.rollConditions(mapId, rng)`가 seed로 조건을 굴리므로 같은 seed면 재현된다 (Math.random을 쓰지 않는다 — 원칙 2). 바람은 화면 왼쪽 위 상자에 방향 화살표와 풍속 숫자(1~5)로 표시한다. 풍속을 숫자로 보이되 m/s 같은 물리 단위는 쓰지 않아 PRD 2.3의 금지 범위를 지켰다.

**클리어 가능성 재검증** — 목표물이 랜덤화되므로 한 지점이 아니라 랜덤화 공간 전체가 클리어 가능해야 한다. `tools/calibrate.html`을 고쳐, 25조합 각각을 8개 리스폰 표본으로 굴려 모든 표본에서 명중 해가 존재함을 실증했다 (전항 통과, Map 5 최상급도 최소 9개 해).
